import 'package:flutter/material.dart';

/// Application color constants
/// Centralized color definitions for consistent theming across the app
class AppColors {
  AppColors._();

  // Background colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color lightGray = Color(0xFFEFF3F4);

  // Primary colors
  static const Color accentBlue = Color(0xFF07899E);
  static const Color primaryText = Color(0xFF07899E);

  // Text colors
  static const Color textPrimary = Color(0xFF000000);

  // Shadow colors
  static const Color shadowLight = Color(0x0D000000);
  static const Color shadowMedium = Color(0x14000000);

  // Transparent
  static const Color transparent = Colors.transparent;
}

